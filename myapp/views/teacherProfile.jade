extends teacherSidebar

block content
    if user
        h1= 'hello! ' + user.name
    else
        h1= 'no user'
    #profile
        ul
            li= '姓名 ' + user.name
            li= '学号 ' + user.id
            li= '邮箱 ' + user.email
            li= '权限 ' + user.identity


